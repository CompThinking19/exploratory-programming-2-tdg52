# defined an argument as a list
# created a TypeError for non-lists
def greerboys(numbers)
if type(greerboys) != list:
  raise TypeError("not the boys")
for element in numbers:
  if element >= 0:
    return yes
greerboys([1, 2, 3])
greerboys("not the boys")
